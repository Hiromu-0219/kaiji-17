# 十七歩 卓上計時盤

実牌を使う二人麻雀「17歩」向けの横画面専用PWAタイマーです。自風の設定・重複なしランダム抽選、30秒単位の制限時間、ローカルルールメモ、カウントダウン、時間帯別演出、一時停止・再開・リセット、Wake Lock、効果音・振動に対応しています。設定は端末の `localStorage` に保存されます。

## 起動

```bash
npm install
npm run dev
```

ブラウザで表示されたローカルURLを開き、端末を横向きにしてください。

## ビルド

```bash
npm run build
```
